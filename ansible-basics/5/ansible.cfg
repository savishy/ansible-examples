[defaults]
# disable host-key checking. if you enable this,
# auto-generated hosts (e.g. docker hosts) might ask to
# verify identity.
# http://stackoverflow.com/a/32338480/682912

host_key_checking = false
inventory = ./inventory
